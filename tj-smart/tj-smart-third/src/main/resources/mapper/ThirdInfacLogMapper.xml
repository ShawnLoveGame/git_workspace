<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ai.smart.third.confiscation.mapper.LoggingMapper" >

	<sql id="BaseColumn">
          id     id,
          method_name     methodName,
          serial_num     serialNum,
          remark     remark,
          return_content     returnContent,
          create_time     createTime
    </sql>

    <insert id="WriteLog" parameterType="com.ai.smart.third.confiscation.model.ThirdInfacLog">
    insert into third_infac_log (
          method_name,
          serial_num,
          remark,
          return_content,
          create_time
      )
    values (
           #{methodName},
           #{serialNum},
           #{remark},
           #{returnContent},
           #{createTime}
      )
    </insert>

</mapper>   
